# 抖音视频上传使用说明

## 📋 流程概述

由于抖音的 Cookie 认证机制比较复杂，需要以下步骤：

1. **步骤 1**：运行登录脚本，手动登录抖音
2. **步骤 2**：保存登录后的 Cookie
3. **步骤 3**：使用保存的 Cookie 上传视频

---

## 🚀 开始使用

### 第 1 步：运行登录脚本

```bash
python3 douyin_login.py
```

**操作说明**：
1. 浏览器会自动打开并访问抖音创作者中心
2. 如果显示登录页面，使用手机扫码登录
3. 登录成功后，按 **Enter** 键继续
4. 脚本会自动保存 Cookie 到 `douyin_cookies.json` 文件

**预期输出**：
```
🎬 抖音登录助手

📍 访问登录页面...
...
👆 登录完成后，按 Enter 键继续...

💾 导出 Cookie...
✅ Cookie 已保存到: douyin_cookies.json
   共 X 个 Cookie
```

---

### 第 2 步：测试 Cookie 有效性（可选但推荐）

```bash
python3 douyin_login.py --test
```

**预期输出**：
```
🧪 测试 Cookie 有效性

📅 Cookie 创建时间: 2026-01-19T...
🍪 Cookie 数量: 10

✅ Cookie 有效
   URL: https://creator.douyin.com/creator-micro/home
   标题: 抖音创作者中心
```

**如果显示 "Cookie 已过期"**：
- 请重新运行第 1 步，重新登录

---

### 第 3 步：上传视频

#### 单个视频

```bash
python3 douyin_uploader_with_login.py \
    --video src/data/videos/sasuke_specific_1768745166.mp4 \
    --title "火影忍者 AI 视频" \
    --topics "#火影忍者,#动漫,#AI视频"
```

#### 使用默认配置

```bash
python3 douyin_uploader_with_login.py \
    --video src/data/videos/sasuke_specific_1768745166.mp4
```

---

## 📁 文件说明

| 文件 | 说明 |
|------|------|
| `douyin_login.py` | 登录脚本，引导用户手动登录并保存 Cookie |
| `douyin_uploader_with_login.py` | 上传脚本，使用保存的 Cookie 上传视频 |
| `douyin_cookies.json` | 保存的 Cookie 文件（登录后自动生成） |

---

## ⚠️ 注意事项

1. **Cookie 有效期**
   - Cookie 通常在 7 天内有效
   - 如果上传失败提示 "Cookie 已过期"，请重新登录

2. **浏览器操作**
   - 登录脚本必须使用 `headless=False`，以便显示登录二维码
   - 上传脚本可以使用 `headless=False` 观察上传过程

3. **网络问题**
   - 确保网络连接稳定
   - 抖音服务器可能有限流，请勿频繁上传

4. **文件要求**
   - 视频格式：MP4、MOV、AVI
   - 视频大小：建议小于 500MB
   - 视频时长：建议 15-60 秒

---

## 🔧 常见问题

### Q1: 登录时显示"需要验证码"？

**A**: 这是抖音的安全机制。请使用手机抖音 App 扫描二维码登录，不要使用账号密码登录。

### Q2: Cookie 保存后上传仍然失败？

**A**: 尝试以下步骤：
1. 删除 `douyin_cookies.json` 文件
2. 重新运行登录脚本
3. 确保登录后页面完全加载（等待 3-5 秒）
4. 再试上传

### Q3: 能否让登录脚本自动完成登录？

**A**: 出于安全考虑，抖音不支持自动化登录。必须手动扫码登录。

### Q4: 上传时找不到上传按钮？

**A**: 这可能是因为：
1. Cookie 已过期（请重新登录）
2. 页面加载未完成（等待更长时间）
3. 抖音页面结构变更（需要更新选择器）

---

## 📞 获取帮助

如果遇到问题：
1. 检查日志输出
2. 尝试重新登录
3. 确保视频文件存在且格式正确

**祝你使用愉快！** 🎉
