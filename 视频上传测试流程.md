# 抖音视频上传测试流程

## 📋 目录

- [环境检查](#环境检查)
- [测试流程](#测试流程)
- [常见问题](#常见问题)
- [解决方案](#解决方案)

---

## 🔍 环境检查

### 1. 检查 Chrome 是否已登录

```bash
# 运行浏览器登录工具
python3 douyin_browser.py
```

**预期结果**：
- ✅ 打开浏览器窗口
- ✅ 访问抖音网页版
- ✅ 显示已登录状态（Cookie 数量 > 0）
- ✅ 检测到登录特征（我的订单、创作者中心、消息）

### 2. 关闭所有 Chrome 进程

```bash
# 关闭 Chrome
pkill -f "Google Chrome"

# 验证
if pgrep -f "Chrome" > /dev/null; then
    echo "❌ 仍有 Chrome 进程"
else
    echo "✅ 所有 Chrome 进程已关闭"
fi
```

### 3. 检查视频文件

```bash
# 查找视频文件
find . -name "*.mp4" -type f

# 检查大小
ls -lh src/data/videos/
```

---

## 🧪 测试流程

### 步骤 1：基础浏览器测试

```bash
python3 << 'EOF'
import sys
import time
from pathlib import Path

print("="*70)
print("🧪 基础浏览器测试")
print("="*70)

try:
    from playwright.sync_api import sync_playwright
    
    user_data_dir = "/Users/ygh/Library/Application Support/Google/Chrome/Default"
    
    print(f"\n📂 使用: {user_data_dir}")
    
    playwright = sync_playwright().start()
    context = playwright.chromium.launch_persistent_context(
        user_data_dir=user_data_dir,
        headless=True,
        timeout=30000,
    )
    
    page = context.new_page()
    page.goto("https://www.douyin.com/")
    time.sleep(3)
    
    title = page.title()
    print(f"\n📊 页面标题: {title}")
    
    if "抖音" in title:
        print("✅ 访问成功")
    else:
        print("⚠️ 可能需要验证码")
    
    context.close()
    playwright.stop()
    
except Exception as e:
    print(f"❌ 测试失败: {e}")
    sys.exit(1)

print("\n✅ 测试完成")
EOF
```

### 步骤 2：视频上传测试

```bash
# 使用 v2 脚本上传视频
python3 douyin_upload_video_v2.py \
    --video src/data/videos/sasuke_specific_1768745166.mp4 \
    --title "测试视频" \
    --topics "#测试,#AI"
```

---

## ❓ 常见问题

### 问题 1：验证码检测

**症状**：
```
📊 页面标题: 验证码中间页
```

**原因**：
- Playwright 使用的 Chromium 被检测为自动化工具
- 抖音的安全系统触发了验证码

**解决方案**：
1. 使用 v2 脚本（已隐藏自动化特征）
2. 在普通 Chrome 中先完成验证码验证
3. 等待一段时间后重试

### 问题 2：浏览器启动失败

**症状**：
```
❌ BrowserType.launch_persistent_context: Failed to create a ProcessSingleton
```

**原因**：
- Chrome 进程仍在运行
- 配置文件被锁定

**解决方案**：
```bash
# 关闭所有 Chrome 进程
pkill -f "Google Chrome"
pkill -f "Chromium"

# 等待 5 秒
sleep 5

# 重试
python3 douyin_upload_video_v2.py --video <视频路径>
```

### 问题 3：找不到上传元素

**症状**：
```
❌ 未找到文件上传元素
```

**原因**：
- 上传页面结构变化
- 页面未完全加载

**解决方案**：
1. 增加等待时间
2. 检查页面截图：`debug_upload_error.png`
3. 手动访问上传页面查看结构

### 问题 4：Cookie 过期

**症状**：
```
🔍 登录状态检查:
   状态: 需要登录
```

**解决方案**：
1. 关闭所有 Chrome 窗口
2. 手动在 Chrome 中登录抖音
3. 重新运行脚本

---

## 🔧 解决方案总结

### 方案 1：关闭 Chrome 后重试

```bash
# 1. 关闭 Chrome
pkill -f "Google Chrome"

# 2. 等待
sleep 5

# 3. 重新登录（如果需要）
python3 douyin_browser.py

# 4. 上传视频
python3 douyin_upload_video_v2.py --video <视频路径>
```

### 方案 2：使用无头模式测试

```bash
# 先用无头模式测试基础功能
python3 douyin_upload_video_v2.py \
    --video <视频路径> \
    --title "测试" \
    --headless
```

### 方案 3：手动验证

```bash
# 1. 手动打开 Chrome 登录抖音
open -a "Google Chrome"

# 2. 确保登录成功

# 3. 关闭 Chrome

# 4. 运行脚本
python3 douyin_upload_video_v2.py --video <视频路径>
```

---

## 📊 测试检查清单

运行测试前，检查以下项目：

- [ ] Chrome 已登录抖音
- [ ] 所有 Chrome 进程已关闭
- [ ] 视频文件存在且小于 500MB
- [ ] Playwright 已安装
- [ ] 网络连接正常

---

## 🎯 下一步

1. **基础测试**：
   ```bash
   python3 douyin_browser.py
   ```

2. **上传测试**：
   ```bash
   python3 douyin_upload_video_v2.py \
       --video src/data/videos/sasuke_specific_1768745166.mp4 \
       --title "我的测试视频" \
       --topics "#测试,#AI"
   ```

3. **查看日志**：
   ```bash
   cat debug_*.png  # 查看截图
   ```

---

**祝测试顺利！** 🎉
