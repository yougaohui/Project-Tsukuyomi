# 🎬 抖音视频上传 - 替代方案

由于抖音的安全机制，直接自动化上传可能遇到验证码问题。以下是几种替代方案：

---

## 方案 1：PyAutoGUI（模拟鼠标键盘操作）

### 安装
```bash
pip install pyautogui pyperclip
```

### 使用
```bash
python3 upload_with_pyautogui.py \
    --video src/data/videos/test.mp4 \
    --title "我的视频"
```

### 特点
- ✅ 模拟真实的鼠标点击和键盘输入
- ✅ 不容易被检测
- ❌ 需要手动调整坐标
- ❌ 需要浏览器窗口在前台
- ❌ 需要手动选择文件

---

## 方案 2：浏览器控制台脚本（最可靠）

### 使用方法
1. 在 Chrome 中打开抖音创作者中心并登录
2. 按 F12 打开开发者工具
3. 切换到 Console 标签
4. 粘贴 `browser_console_upload.js` 的内容
5. 按 Enter 执行

### 特点
- ✅ 在真实浏览器环境中运行
- ✅ 不会触发验证码
- ✅ 可以访问所有页面元素
- ❌ 需要手动复制粘贴代码
- ❌ 部分操作可能受限（文件选择）

---

## 方案 3：AppleScript（macOS 原生）

### 使用方法
1. 打开"脚本编辑器"（Script Editor）
2. 打开 `upload_with_applescript.applescript`
3. 修改配置（视频路径、标题等）
4. 运行脚本

### 特点
- ✅ macOS 原生支持
- ✅ 系统级自动化
- ✅ 可以控制其他应用
- ❌ 需要辅助功能权限
- ❌ 网页操作有限

---

## 方案 4：手动操作 + 脚本辅助

### 流程
1. **手动**：打开浏览器，登录抖音，打开上传页面
2. **脚本**：辅助填写标题、话题
3. **手动**：选择文件，确认发布

### 示例
```bash
# 启动辅助模式（监听剪贴板）
python3 upload_assistant.py --watch

# 在浏览器中上传时：
# 1. 手动选择文件
# 2. 脚本自动检测并填写标题
# 3. 手动点击发布
```

---

## 方案 5：使用抖音官方 API

### 要求
1. 注册抖音开放平台账号
2. 创建应用
3. 申请 `video.create.bind` 权限
4. 实现 OAuth 2.0 认证
5. 调用官方 API

### API 端点
```
POST https://open.douyin.com/api/douyin/v1/video/upload_video/
```

### 特点
- ✅ 官方支持，最稳定
- ✅ 不会触发验证码
- ✅ 可批量处理
- ❌ 需要审核和权限
- ❌ 需要服务器部署

---

## 推荐方案对比

| 方案 | 稳定性 | 难度 | 验证码 | 推荐场景 |
|------|--------|------|--------|----------|
| 官方 API | ⭐⭐⭐⭐⭐ | 难 | 无 | 批量上传 |
| 控制台脚本 | ⭐⭐⭐⭐ | 中 | 偶尔 | 临时使用 |
| PyAutoGUI | ⭐⭐⭐ | 易 | 无 | 少量上传 |
| AppleScript | ⭐⭐⭐ | 中 | 无 | macOS 用户 |
| 手动辅助 | ⭐⭐⭐⭐ | 易 | 无 | 所有情况 |

---

## 快速开始建议

### 情况 1：偶尔上传
使用 **浏览器控制台脚本**：
1. 手动打开抖音上传页面
2. 在控制台执行脚本
3. 手动选择文件

### 情况 2：批量上传
考虑使用 **抖音官方 API**：
1. 注册开发者账号
2. 申请权限
3. 实现自动化

### 情况 3：不想写代码
使用 **手动辅助模式**：
1. 手动选择文件
2. 脚本辅助填写信息
3. 手动确认发布

---

## 立即尝试

### 最简单的方式：手动 + 脚本辅助

```bash
# 1. 手动在浏览器中打开上传页面
# 2. 选择视频文件
# 3. 运行脚本辅助填写

python3 upload_assistant.py \
    --title "我的视频" \
    --topics "#测试,#AI"
```

---

**选择适合你的方案，开始上传！** 🚀
